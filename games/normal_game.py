"""
Файл запуска обычной версии игры 2048
Запускает классическую версию со случайным появлением двоек
"""

import sys
import os

# Добавляем путь к корневой директории проекта для импорта модулей
sys.path.append(os.path.join(os.path.dirname(__file__), ".."))

from core.normal import NormalGame2048  # noqa: E402
from ui.game_ui import Game2048UI  # noqa: E402

if __name__ == "__main__":
    """
    Точка входа для обычной версии игры 2048
    Создает игровую логику и интерфейс, затем запускает приложение
    """
    # Создание игровой логики для обычной версии
    game_logic = NormalGame2048()

    # Создание пользовательского интерфейса
    app = Game2048UI(game_logic, "2048 - Normal")

    # Запуск главного цикла приложения
    app.run()
